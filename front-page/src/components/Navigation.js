import React from 'react'
import Button from '@mui/material/Button';

const Navigation = () => {
  return (
    <Button className="nav-button" variant="contained" color="primary" sx={{backgroundColor: 'black'}}>
        Hello MUI
    </Button>
  )
}

export default Navigation